{
  "name": "@poker/contracts",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "forge build",
    "abi": "forge build && node script/export-abi.mjs",
    "test": "forge test",
    "format": "forge fmt --check",
    "coverage": "forge coverage"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  }
}
